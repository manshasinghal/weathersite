const AutocorrectInput = (props) => {
    const enterKeyPressed = (event) => {
      if (event.key === 'Enter') {
        event.preventDefault();
        // Handle the enter key press
      }
    };
};

    export default AutocorrectInput;